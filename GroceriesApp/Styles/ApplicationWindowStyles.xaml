<ResourceDictionary x:Class="GroceriesApp.Styles.ApplicationWindowStyles"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:userControls="clr-namespace:GroceriesApp.View.UserControls">
    
    <SolidColorBrush x:Key="Window.Static.Background" Color="#1E1E1E"/>
    <Style x:Key="ApplicationWindow" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource Window.Static.Background}" />
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="10"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContentControl}" >
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" 
                           MouseLeftButtonDown="Window_MouseLeftButtonDown" FrameworkElement.Tag ="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>